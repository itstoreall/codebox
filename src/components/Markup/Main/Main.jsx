import { Routes } from '../../../Routes/Routes';
import Container from '../../Container';

const Main = () => (
  <main className="main">
    <Container>{Routes()}</Container>
  </main>
);

export default Main;
